/**
 * Canonical Drizzle schema is `@dba/shared/db/schema` (shared by Worker + apps).
 * Re-exported here for convenience when navigating under `apps/api/src/db/`.
 */
export * from "@dba/shared/db/schema";
